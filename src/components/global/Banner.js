import React from 'react';
import { imageUrlRecources } from '../../resources/url';

const Banner = () => <img src={imageUrlRecources.banner} className="img-slider" alt="EventCamp" />;

export default Banner;
